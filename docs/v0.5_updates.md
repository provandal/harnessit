# HarnessIT Architecture v0.5 — Document Update Memo

This memo enumerates the changes required to take `HarnessIT_Architecture_v0.4.docx` to v0.5. It is intended to be applied as Word-level edits to v0.4, producing v0.5 as the next published draft.

## Provenance

Source inputs:

- `_reviews/00_synthesis.md` (workspace-level) §1 convergent findings and §6 Priority 2 recommended actions (items 5–16). The synthesis is the primary source for v0.5 deltas; this memo applies it.
- `_reviews/01_devils_advocate.md`, `_reviews/02_agentic_ai_expert.md`, `_reviews/03_rdma_ns3_expert.md`, `_reviews/04_build_pragmatist.md` (workspace-level) — individual reviewer findings underlying the synthesis.
- `doppelganger/spike/decision_memo.md` Finding #1 — the eval-discipline finding that must propagate from substrate-specific to architecture-level.
- 2026-05-03/04 conversation outcomes: Substrate Adapter as named architectural role; the Driver/Adapter internal split inside every Substrate Adapter; the GPL-2.0/Apache-2.0 license boundary that depends on the C++/Python arms-length subprocess+files contract.
- 2026-05-04 capture-protocol decision (`harnessit/docs/v0.3_updates.md`) — drops the `harnessit-build-journal` RememberIT context, which the v0.4 §3.4 references; v0.5 must align.
- 2026-05-04 CSM disclosure constraint (workspace-level memory `feedback_csm_disclosure_line.md`) — the cache-vs-backing-store cleanup is in scope for v0.5; the cache-aware ordering convention sub-item is not.
- 2026-05-04 conversation: Erik confirmed the five sub-decisions that gate v0.5 drafting: skip CSM cache-aware ordering; downgrade multi-agent §5 to "future extension"; eval governance as paragraph not full spec; retrieval upgrades acknowledge as v0.6+ work; investigation-checkpoint primitive as full spec in §3.7.

## Summary of changes

| # | Section | Change | Source |
|---|---|---|---|
| 0 | Header | v0.4 → v0.5; status line refresh | Document version |
| 1 | §1, §12 Principle 6 | Drop "harness is the car / model is the engine" metaphor; soften clone-and-run | Synthesis 1.6, 16; DA 2.8 |
| 2 | §2 Claim 1 | Name the moving harness/model boundary | Synthesis 15 / DA 2.8 |
| 3 | §2 Claim 2 | Distinguish fine-tuning-for-domain-expertise from RFT/DPO/distillation-as-continual-improvement | Synthesis 15 / DA 2.8 |
| 4 | §2 Claim 4 | Recast "Skills are the most underused" as a defended hypothesis | Synthesis 15 / DA 2.8 |
| 5 | §3.2 Retrieval | Acknowledge missing 2025–2026 standard practice; defer mechanics to v0.6+ | Synthesis 2.10 (decision D) |
| 6 | §3.3 Skills | Replace "loaded once at start" with progressive-disclosure manifest; add skill composition contract | Synthesis 6 / 2.1 / 2.3 |
| 7 | §3.4 Memory + §3.5 CSM | Disambiguate as backing-store-vs-cache; drop the now-defunct `harnessit-build-journal` context reference | Synthesis 8 / 2.5 + v0.3 capture-protocol decision |
| 8 | §3.6 Orchestration | Add Router/Classifier as named sub-component; add bisection / structured investigation as third pattern | Synthesis 5 / 2.2 + Synthesis 9 / 2.6 |
| 9 | §3.7 Gates | Specify gate-denial contract; add investigation-checkpoint primitive | Synthesis 10 / 2.7 (decision E) |
| 10 | §3.8 Evals | Add eval-governance paragraph; new §3.8.x Eval Discipline (incomplete-flow accounting) | Synthesis 11 + spike Finding #1 (decision C) |
| 11 | §4.1 Substrate Tier | Introduce Substrate Adapter as named architectural role with internal Driver/Adapter split; add RememberIT same-author honesty | Session-B refinement + Synthesis 13 / 1.7 |
| 12 | §5 Multi-Agent | Downgrade from "from day one, used lightly at first" to "future extension; v1 is single-agent" | Synthesis 14 / 1.9 (decision B) |
| 13 | §6.1 Doppelgänger | Acknowledge post-mortem-only as fundamental limitation; reference live-feel replay as Doppelgänger-side mitigation; cross-reference Substrate Adapter | Synthesis 1.5 + session-B refinement |
| 14 | §9.4 OTel | Resolve standards-engagement contradiction: drop "contribute back" + commit to stable internal namespace with migration plan | Synthesis 12 |
| 15 | §10.1 Landscape | Acknowledge RememberIT as same-author dependency under Principle 7 | Synthesis 1.7 / 13 |
| 16 | §11 Roadmap | Resolve 8-stage-vs-14-stage mismatch with explicit mapping paragraph | Synthesis 1.8 / Priority 1 #4 |
| 17 | §13 Open Questions | Convert punts to known unknowns with deadlines or resolve | Synthesis 2.9 |

---

## Update 0 — Header / version block

### Current text in v0.4

> HarnessIT
> Architecture Overview
> Building agentic harnesses that outlive the models they wrap
> Draft v0.4
> provandal.dev
> Status: Design north star for the HarnessIT project

### Proposed v0.5 text

> HarnessIT
> Architecture Overview
> Building agentic harnesses that outlive the models they wrap
> Draft v0.5
> provandal.dev
> Status: Design north star for the HarnessIT project. v0.5 absorbs the 2026-05-01 four-reviewer pressure-test findings and the architectural refinements that emerged during the 2026-05 design sessions (Substrate Adapter as a named architectural role; Driver/Adapter internal split; eval-discipline propagated from the Doppelgänger fork spike).

### Rationale

Version bump and a one-sentence status note that names the v0.5 inputs without naming the changes themselves. Helps a returning reader orient on what's new.

---

## Update 1 — §1 + §12 Principle 6: drop metaphor, soften clone-and-run

### Current text in v0.4

§1, last sentence of penultimate paragraph:

> The model is the engine. The harness is the car.

§1, paragraph after that:

> HarnessIT has three goals. First, to be a working agentic harness for a real domain, network troubleshooting on a simulated RoCE fabric, that someone can clone, run, and learn from. Second, to be a teaching artifact, accompanied by a deep-dive blog series that walks through every component, every decision, and every failure mode along the way. Third, to be a reference architecture that holds up under scrutiny, opinionated where opinions are warranted, neutral where they are not, and honest about what is hard.

§12 Principle 6:

> Principle 6: The harness must be reproducible.
>
> If a reader cannot clone the repository at the state corresponding to a post and run the example, the project has failed. Each post's end state is a tagged commit.

### Proposed v0.5 text

§1, replace the metaphor sentence — delete it entirely; the paragraph that follows already names the harness without the metaphor.

§1, replace the three-goals paragraph:

> HarnessIT has three goals. First, to be a working agentic harness for a real domain — network troubleshooting on a simulated RoCE fabric — that someone can clone and run, with a clear-eyed README about what setup actually takes. Second, to be a teaching artifact, accompanied by a deep-dive blog series that walks through every component, every decision, and every failure mode along the way. Third, to be a reference architecture that holds up under scrutiny, opinionated where opinions are warranted, neutral where they are not, and honest about what is hard.

§12 Principle 6:

> Principle 6: The harness must be reproducible, with honesty about setup.
>
> A reader must be able to clone the repository at the state corresponding to a post and reproduce the example. Each post's end state is a tagged commit. *Reproducible* does not mean *zero-friction*: HarnessIT consumes multiple substrate dependencies (Doppelgänger Docker image, self-hosted Langfuse stack, RememberIT credentials, model API key, knowledge corpus). The README owns this — "30 minutes to a running Doppelgänger via `docker pull`; 2–4 hours to a working harness end-to-end; expect to debug the substrate stack on an unfamiliar host" is fine. *Clone-and-run* implies `docker compose up`, which the project is not.

### Rationale

The harness/car metaphor inverts the asymmetry — a model without a harness is already useful; a harness without a model is nothing — and pre-litigates the architecture in slogan form. Drop it from the doc; it can survive in a blog post if it earns its keep there. The clone-and-run softening reflects synthesis 1.6's three-reviewer convergence: the realistic clone-and-run audience is approximately zero unless the README owns the setup. Both fixes are honesty under Principle 7.

---

## Update 2 — §2 Claim 1

### Current text in v0.4

> Claim 1: Frontier base models bring general competence; the harness brings everything else.
>
> Frontier models trained through 2025 and 2026 have substantial general competence in well-documented technical domains. They have read the standards, the textbooks, the protocol specifications, the major vendor documentation. What they lack is exactly what makes domain expertise actually useful: access to the specific environment, the current state of the live system, the operational discipline accumulated by working teams, and the authoritative recency that comes from looking up the source rather than recalling from training data.
>
> The harness fills these four gaps. Tools provide access to the environment. Sensing provides current state. Skills encode operational discipline. Retrieval provides authoritative recency. None of these are problems fine-tuning can solve well. All of them are problems the harness solves naturally. The model and the harness are complements, not competitors.

### Proposed v0.5 text

> Claim 1: Frontier base models bring general competence; the harness brings everything else — for now.
>
> Frontier models trained through 2025 and 2026 have substantial general competence in well-documented technical domains. They have read the standards, the textbooks, the protocol specifications, the major vendor documentation. What they lack is exactly what makes domain expertise actually useful: access to the specific environment, the current state of the live system, the operational discipline accumulated by working teams, and the authoritative recency that comes from looking up the source rather than recalling from training data.
>
> The harness fills these four gaps. Tools provide access to the environment. Sensing provides current state. Skills encode operational discipline. Retrieval provides authoritative recency. None of these are problems fine-tuning can solve well; all of them are problems the harness solves naturally. The model and the harness are complements, not competitors.
>
> The harness/model boundary is not stable across model generations. Tool use was harness work in 2023 and is increasingly model-native in 2025. Long-context handling has shifted some of what CSM does into the model itself. Frontier capability moves the boundary every generation. The leverage stays in the harness because each generation's boundary leaves new gaps where harness work compounds — but the *specific* components that carry the leverage will shift. v0.5 of HarnessIT names the components that matter at the time of writing, not for all time.

### Rationale

The synthesis flagged Claim 1 as unfalsifiable: without naming where the harness/model boundary will move, "harness is where the leverage lives" is a slogan. The added paragraph names the moving boundary explicitly and reframes the claim as time-indexed. Doesn't weaken the claim — strengthens it by making it falsifiable.

---

## Update 3 — §2 Claim 2

### Current text in v0.4

> Claim 2: Fine-tuning solves a problem most teams do not have.
>
> Fine-tuning is appropriate for narrow output-format reliability, for high-volume cost optimization through distillation, and for specific structured-output workflows where format adherence matters more than reasoning. It is not appropriate as a primary investment to make a model an expert in a domain where general competence already exists. Teams that fine-tune for domain expertise pay the tax repeatedly as base models improve.

### Proposed v0.5 text

> Claim 2: Fine-tuning *for domain expertise* solves a problem most teams do not have.
>
> Fine-tuning has three legitimate roles in 2025–2026: narrow output-format reliability (structured outputs, function-call schemas), high-volume cost optimization through distillation of a frontier model into a cheaper one, and reinforcement-from-trajectories (RFT) or direct preference optimization (DPO) that uses successful agent trajectories as continual-improvement signal. These are real and sometimes necessary.
>
> The mistake HarnessIT argues against is the *fourth* use: fine-tuning a model on domain documents and runbooks to make it an expert in a domain where general competence already exists. Teams that fine-tune for domain expertise pay the tax repeatedly as base models improve, while teams that invest in retrieval, skills, and tools see the same investment carry across model generations. The "harness-first" position is "harness as primary investment, then selective post-training of trajectories that survive eval scrutiny" — not "no fine-tuning ever."

### Rationale

The synthesis flagged Claim 2 as a strawman: it lumps RFT/DPO/distillation in with "fine-tuning bad" and never commits to the narrower "harness-first, then selective post-training" position the architecture actually holds. The revision distinguishes the four uses explicitly and lands the position the rest of the document implies.

---

## Update 4 — §2 Claim 4

### Current text in v0.4

> Claim 4: Skills are the most underused component.
>
> Most teams understand tools and RAG. Many have memory of some kind. Few have figured out skills, loadable, composable procedural instructions that the agent can pull in when the task calls for them. Skills are how you encode operational discipline without baking it into prompts that grow uncontrollably. They are the highest-leverage component most teams are not yet using well.

### Proposed v0.5 text

> Claim 4 (hypothesis): Skills are underused relative to their leverage in production agents in 2025–2026.
>
> The strong form — "Skills are *the most* underused component" — is asserted rather than defended. v0.5 retains the spirit but recasts it as a hypothesis the project will pressure-test through the build. The observable evidence: most production agents in 2025–2026 ship with tools and RAG; many have memory of some kind; few ship with the loadable, composable, on-demand-loaded procedural instructions that this document calls *skills*. Anthropic's Skills system, Claude Code's subagents, and LangGraph's pattern of dispatching to typed sub-graphs are the closest production analogues.
>
> If skills carry the leverage this document claims they do, the build's eval scores should rise materially when a skill is added relative to comparable RAG additions. The blog series will show the numbers as they happen. If they don't, this claim weakens; honesty about hard parts (Principle 7) takes precedence over preserving the slogan.

### Rationale

The synthesis flagged Claim 4 as folklore: asserted "the most underused" without a baseline. The revision recasts it as a falsifiable hypothesis with a defined evidence path (eval-score deltas in the build). Honest under Principle 2 ("every why is answered") and Principle 7 ("honesty about hard parts").

---

## Update 5 — §3.2 Retrieval (RAG)

### Current text in v0.4

> 3.2 Retrieval (RAG)
>
> What the agent can look up. Retrieval is the agent's library.
>
> In HarnessIT. A vector store over networking knowledge, RFCs, IBTA RoCE specifications, vendor configuration guides, published runbooks, and a separate structured-knowledge layer for things that are graphs rather than prose (the relationship between PFC, ECN, DCQCN, and lossless queue behavior, for example). The agent retrieves prose knowledge for definitional and conceptual questions; it traverses the structured layer for relational and dependency questions.
>
> Design principle. Retrieval is for facts. Skills are for procedures. RAG is for "what does ECN-CE marking mean." Skills are for "how do I investigate a PFC storm." Conflating the two is a common harness mistake; they have different content shapes, different update cadences, and different consumption patterns.

### Proposed v0.5 text

> 3.2 Retrieval (RAG)
>
> What the agent can look up. Retrieval is the agent's library.
>
> In HarnessIT v0.5. A vector store over networking knowledge — RFCs, IBTA RoCE specifications, vendor configuration guides, published runbooks — and a separate structured-knowledge layer for things that are graphs rather than prose (the relationship between PFC, ECN, DCQCN, and lossless queue behavior, for example). The agent retrieves prose knowledge for definitional and conceptual questions; it traverses the structured layer for relational and dependency questions.
>
> Design principle. Retrieval is for facts. Skills are for procedures. RAG is for "what does ECN-CE marking mean." Skills are for "how do I investigate a PFC storm." Conflating the two is a common harness mistake; they have different content shapes, different update cadences, and different consumption patterns.
>
> What v0.5 does *not* yet specify. The vector-only design above is honest to v0.4's commitment but is a 2023-era pattern. 2025–2026 production retrieval includes: hybrid retrieval (BM25 lexical + vector semantic, fused with reciprocal rank or weighted scoring), reranking (cross-encoder re-scoring of top-K candidates), query rewriting (LLM-driven multi-query, HyDE for hypothetical-document expansion), and reranker-aware chunking strategies. v0.5 acknowledges these as standard practice missing from v0.4; v0.6 (or stage-6 retrieval work in the build, whichever comes first) will absorb them. Until then, the v0.5 retrieval layer is intentionally a baseline against which the upgrades can be measured.

### Rationale

The synthesis flagged §3.2 as the architecture's weakest section relative to current standard practice (Synthesis 2.10). v0.5 acknowledges the gap explicitly without rushing the mechanics. The "intentionally a baseline" framing turns a weakness into a measurement opportunity for the build.

---

## Update 6 — §3.3 Skills (progressive disclosure + composition contract)

### Current text in v0.4

> 3.3 Skills
>
> Procedural knowledge the agent loads on demand. Skills are the agent's playbook.
>
> In HarnessIT. Skills live in a separate project named TheConstruct, packaged independently so other harnesses can use them. Each skill is a self-contained instruction module for a specific task class. "Investigate RDMA Timeout." "Diagnose PFC Storm." "Verify Config Change." "Triage GPU Job Failure." Skills are loaded based on task classification at the start of an investigation and stay resident in context for the duration. They encode the order of operations, the diagnostic discipline, the bisection strategy, the success criteria, the things that distinguish a senior engineer's approach from a junior engineer's.
>
> Design principle. Skills are not prompts. A prompt grows uncontrollably as you try to handle more cases; a skill is bounded and reusable. Skills compose: "Verify Config Change" can be invoked from inside "Apply Hotfix." Skills are versioned and tested against the eval set. The single most surprising lesson of harness design is how much leverage a well-written skill provides for the same model and the same tools.

### Proposed v0.5 text

> 3.3 Skills
>
> Procedural knowledge the agent loads on demand. Skills are the agent's playbook.
>
> In HarnessIT. Skills live in a separate project named TheConstruct, packaged independently so other harnesses can use them. Each skill is a self-contained instruction module for a specific task class — *Investigate RDMA Timeout*, *Diagnose PFC Storm*, *Verify Config Change*, *Triage GPU Job Failure*. Skills encode the order of operations, the diagnostic discipline, the bisection strategy, the success criteria — the things that distinguish a senior engineer's approach from a junior engineer's.
>
> **Loading discipline: progressive disclosure, not loaded-at-start.** v0.4 specified that skills are loaded at the start of an investigation based on task classification and stay resident for the duration. v0.5 corrects this: at investigation start, the agent sees a *manifest* — a short list of available skills and their one-line descriptions — and loads a skill's full body on demand the first time it is invoked. The full body stays resident only as long as the investigation is exercising it; CSM (§3.5) is responsible for evicting stale skill bodies just as it does any other context content.
>
> Two reasons for the change. First, real investigations pivot mid-task — a "PFC storm" investigation that bisects into "buffer misconfiguration" halfway through needs the second skill loaded at the moment the framing shifts, not retroactively wished into the original load decision. Loaded-at-start is structurally backwards for that pattern. Second, skill bodies are some of the highest-token-cost content the agent handles; progressive disclosure is the natural budget mechanism. Anthropic's Skills system, Claude Code's subagents, and LangGraph's typed sub-graph dispatch all converged on progressive disclosure for the same reasons.
>
> **Composition contract.** Skills compose. *Verify Config Change* can be invoked from inside *Apply Hotfix*. v0.5 specifies the contract:
>
> - **Invocation.** Skills are invoked through a typed tool call: `invoke_skill(name, params) -> SkillResult`. The model does not free-form import a skill into its prompt; the orchestrator dispatches the call and the result returns through the same tool-result pathway as any other tool.
> - **Composition shape.** Composition is by call stack, not context merge. The inner skill runs with its own context window slice (managed by CSM); when it returns, control returns to the outer skill with a `SkillResult` payload. The outer skill does not see the inner skill's intermediate reasoning unless `SkillResult` exposes it explicitly.
> - **Return contract.** The calling skill names the shape of `SkillResult` it expects. Mismatches are gate-blockers: the orchestrator surfaces the contract violation rather than letting the calling skill operate on a malformed return.
>
> Skills are versioned and tested against the eval set. The single most surprising lesson of harness design — to be confirmed in the build, see Claim 4 in §2 — is how much leverage a well-written skill provides for the same model and the same tools.

### Rationale

The synthesis (Priority 2 #6, drawn from Agentic 2.1 and 2.3) identified two structurally important fixes here: progressive disclosure replaces loaded-at-start (agentic 2.1; matches Anthropic Skills, Claude Code subagents, LangGraph dispatch); composition contract makes "skills compose" buildable rather than aspirational. Both fixes are specifications, not new mechanism — the architecture already gestured at them; v0.5 commits.

---

## Update 7 — §3.4 Memory + §3.5 CSM (cache-vs-backing-store cleanup; drop defunct context)

### Current text in v0.4

§3.4:

> 3.4 Memory
>
> What the agent remembers across turns and sessions. Memory is the agent's experience.
>
> In HarnessIT. A two-tier memory system backed by RememberIT, a cloud-hosted MCP context service with client-side encryption. Short-term memory holds the active investigation: what has been observed, what has been ruled out, what the current hypothesis is. Long-term memory accumulates incidents, resolutions, and learned patterns across sessions. "We saw this same symptom on rack 7 in March; the cause was a flaky transceiver on the leaf-3 uplink." Long-term memory is consulted at the start of an investigation; short-term memory is the working substrate during it.
>
> Why RememberIT. Rather than build a memory system from scratch, HarnessIT consumes RememberIT as its memory substrate. RememberIT is already production-deployed at api.rmit.io, already provides client-side encryption, already exposes an MCP interface, and already handles the operational concerns (replication, retention, access control) that a memory system needs. This is the same discipline applied elsewhere in the architecture: consume existing systems rather than rebuild what already works.
>
> Two contexts, one substrate. HarnessIT uses two distinct RememberIT contexts. The first, harnessit-agent-memory, holds operational memory consulted by the agent during investigations. The second, harnessit-build-journal, holds the build-process capture (decisions, surprises, insights) discussed in the build plan. Same substrate, separate namespaces, different access patterns and retention policies. Mixing them would create noise in both directions.
>
> Design principle. Memory is what makes the agent compound. Each session should make the next session better. The hardest design decision in memory is what to remember: raw transcripts are too much, summaries lose nuance, structured extractions are brittle. HarnessIT will use a hybrid approach and the blog series will show why none of the simple answers work.

§3.5:

> 3.5 Context Management
>
> What is in the model's context window at any given moment, and who decides. Context management is the agent's attention.
>
> In HarnessIT. A Context State Manager (CSM) that owns the contents of the model's context window across an investigation. CSM decides what stays, what gets compressed, what gets evicted, and what gets re-loaded when the model needs it again. It tracks token budgets per content class, observations, retrieved knowledge, skill instructions, memory snippets, screen captures, conversation history, and enforces them.
>
> Why this matters. Naive harnesses grow the context window monotonically until they hit the limit and either crash or start dropping content arbitrarily. Real investigations on real fabrics produce more data than fits in any context window. CSM is what makes long investigations tractable. It is also what makes screen capture viable: a single screenshot can cost 1,500 to 3,000 tokens, and an investigation that captures the screen even occasionally will overflow without disciplined management.
>
> Design principle. Context is a budget, not a buffer. The harness must explicitly decide what is worth its space, with the same discipline a kernel applies to physical memory. CSM is one of the components most teams underbuild.

### Proposed v0.5 text

§3.4:

> 3.4 Memory
>
> What the agent remembers across turns and sessions. Memory is the agent's experience — the *backing store* that survives across context-window boundaries.
>
> In HarnessIT. A two-tier memory system backed by RememberIT, a cloud-hosted MCP context service with client-side encryption. Short-term memory holds the active investigation: what has been observed, what has been ruled out, what the current hypothesis is. Long-term memory accumulates incidents, resolutions, and learned patterns across sessions: *We saw this same symptom on rack 7 in March; the cause was a flaky transceiver on the leaf-3 uplink.* Long-term memory is consulted at the start of an investigation; short-term memory is the working substrate during it.
>
> **Memory and CSM (§3.5) are distinct components with distinct roles. Memory is the backing store; CSM is the cache.** When CSM evicts content from the active context window, evicted content lands in short-term memory. When CSM needs that content again later in the investigation, it pulls from short-term memory. The architecture relies on the cache-vs-backing-store cut being clean: confusion between them produces both eviction loops and untracked memory growth.
>
> Why RememberIT. Rather than build a memory system from scratch, HarnessIT consumes RememberIT as its memory substrate. RememberIT is already production-deployed at api.rmit.io, already provides client-side encryption, already exposes an MCP interface, and already handles the operational concerns (replication, retention, access control) that a memory system needs. This is the same discipline applied elsewhere in the architecture: consume existing systems rather than rebuild what already works.
>
> One context, one substrate. HarnessIT uses one RememberIT context for memory: `harnessit-agent-memory` — operational memory consulted by the agent during investigations and accumulated across them. (The build plan's capture protocol uses a flat workspace-level `journal.md`, not a RememberIT context; v0.4 had a second `harnessit-build-journal` context that v0.5 retires.)
>
> Design principle. Memory is what makes the agent compound. Each session should make the next session better. The hardest design decision in memory is what to remember: raw transcripts are too much, summaries lose nuance, structured extractions are brittle. HarnessIT will use a hybrid approach and the blog series will show why none of the simple answers work.

§3.5:

> 3.5 Context Management
>
> What is in the model's context window at any given moment, and who decides. Context management is the agent's attention — the *cache* on top of Memory.
>
> In HarnessIT. A Context State Manager (CSM) that owns the contents of the model's context window across an investigation. CSM decides what stays, what gets compressed, what gets evicted, and what gets re-loaded when the model needs it again. It tracks token budgets per content class — observations, retrieved knowledge, skill instructions, memory snippets, screen captures, conversation history — and enforces them.
>
> **CSM and Memory (§3.4) are distinct components.** CSM is the cache that holds the working subset of the investigation's state in the model's context window; Memory is the backing store that holds everything else, including content CSM has previously evicted. Investigation state lives in Memory (specifically short-term memory); CSM holds the working subset. When CSM evicts, content does not disappear; it goes to Memory. When CSM needs it again, it pulls from Memory. The cache/backing-store cut is the single most important architectural distinction between the two components.
>
> Why this matters. Naive harnesses grow the context window monotonically until they hit the limit and either crash or start dropping content arbitrarily. Real investigations on real fabrics produce more data than fits in any context window. CSM is what makes long investigations tractable. It is also what makes screen capture viable: a single screenshot can cost 1,500 to 3,000 tokens, and an investigation that captures the screen even occasionally will overflow without disciplined management.
>
> Design principle. Context is a budget, not a buffer. The harness must explicitly decide what is worth its space, with the same discipline a kernel applies to physical memory. CSM is one of the components most teams underbuild.

### Rationale

Two fixes folded into one update. (a) The cache-vs-backing-store cut (synthesis 2.5) clarifies an existing fuzzy boundary between Memory and CSM without revealing CSM internals (no eviction algorithm, no scoring function, no token-budget formulas — those stay private per `feedback_csm_disclosure_line.md`). It is a clarification of two existing public components' relationship, not new internals. (b) Drops the `harnessit-build-journal` RememberIT context that v0.4 §3.4 references — the v0.3 capture protocol replaced it with a flat workspace-level `journal.md`, and v0.5 must align rather than leave the doc internally inconsistent.

The cache-aware ordering convention sub-item (synthesis 2.4) is **not** included in v0.5 per the disclosure line — the cache-vs-backing-store cleanup addresses the synthesis's clarity concern without adding new internals.

---

## Update 8 — §3.6 Orchestration (Router/Classifier; bisection)

### Current text in v0.4

> 3.6 Planning and Orchestration
>
> How the agent decides what to do next. Orchestration is the agent's executive function.
>
> In HarnessIT. A hybrid orchestration model that selects between two patterns based on the blast radius of the next proposed action.
>
> ReAct as the default. For low-blast-radius work, anything that is reversible and has negligible operational impact, the agent runs in a tight Reason-Act loop. The model thinks, calls a tool, observes the result, thinks again. This covers the vast majority of investigation work and includes reversible actions like clearing counters, running traceroutes, and pulling configuration snapshots. ReAct is fast, adaptive, and well-suited to exploratory work where the right framing of the problem may not be clear up front.
>
> Planner-Actor for high-blast-radius work. When the next proposed action is irreversible or has significant operational impact, applying a configuration change, bouncing an interface in production, reloading a switch, the orchestration mode shifts. A planner produces an explicit plan that is inspectable and auditable before execution. An actor executes one step. A verification ReAct loop confirms the step succeeded as expected. Control returns to the planner if verification fails or if the step revealed information the plan did not anticipate. This pattern combines the discipline of explicit planning with the reactivity of tight verification, and it matches what production change-automation systems have used for years.
>
> Design principle. Orchestration mode is selected by the cost of the next action, not by the phase of the workflow. Investigation and action are interleaved in real troubleshooting; the orchestration must support that interleaving. The blast-radius axis is the right axis because it maps onto the property that actually matters: how much human review is warranted before the agent proceeds.

### Proposed v0.5 text

> 3.6 Planning and Orchestration
>
> How the agent decides what to do next. Orchestration is the agent's executive function.
>
> In HarnessIT. A hybrid orchestration model that selects between three patterns based on the *shape* and the *blast radius* of the next proposed action, dispatched by a named **Router/Classifier** sub-component.
>
> **Router/Classifier sub-component.** The first component the orchestrator invokes at the start of any new task class is the Router/Classifier: it inspects the task framing, selects the orchestration pattern, and resolves which skills' manifest entries are relevant. The classifier's contract is bounded — it returns a `(pattern, candidate_skills, blast_radius_estimate)` tuple — and it is invoked again whenever the task class shifts mid-investigation (a *PFC storm* investigation that bisects into *buffer misconfiguration* halfway through). Without a named classifier, "skills are loaded based on task classification" (§3.3) is aspirational; the classifier is what makes progressive-disclosure dispatch (§3.3) work in practice.
>
> The Router/Classifier is the architecture's eighth component when counted that way; it is described here rather than at top level because its role is exclusively orchestrational dispatch — it consumes inputs from the framing layer and emits dispatch decisions consumed inside the orchestration loop. Other component patterns (LangGraph supervisor agents, CrewAI manager agents, AutoGen GroupChat managers) have converged on naming this role; v0.5 names it for HarnessIT.
>
> **ReAct as the default.** For low-blast-radius work — anything that is reversible and has negligible operational impact — the agent runs in a tight Reason-Act loop. The model thinks, calls a tool, observes the result, thinks again. This covers most exploratory work and includes reversible actions like clearing counters, running traceroutes, and pulling configuration snapshots. ReAct is fast, adaptive, and well-suited to exploratory work where the right framing of the problem may not be clear up front.
>
> **Bisection / Structured Investigation as the third pattern.** A senior network engineer investigating a PFC storm does not "ReAct" through it; they bisect. *Is the pause originating north of the spine or south? Test that. Given south, which leaf? Test that. Given leaf-3, which port? Test that.* This is binary search through a hypothesis space — distinct from both ReAct (which is exploratory and may revisit branches) and Planner-Actor (which is gated on high blast radius and produces an inspectable plan up front). Bisection is read-only or low-blast-radius, like ReAct, but it is *structured*: each step's hypothesis space is partitioned, the test selected, the partition narrowed.
>
> The Router/Classifier dispatches to bisection when the task class is a localization problem with a clear partition function (origin/source/cause-along-a-path). HarnessIT's bisection pattern wraps the loop with explicit hypothesis-space tracking: the agent maintains the partition state, the orchestrator surfaces it for inspection in the trajectory viewer, and a hypothesis-space-empty terminal is treated as a positive finding ("the cause is not in the named partition"). Encoding bisection inside Planner-Actor (which is gated on high blast radius) does not fit because bisection is read-only; encoding it inside ReAct loses the partition-tracking discipline.
>
> **Planner-Actor for high-blast-radius work.** When the next proposed action is irreversible or has significant operational impact — applying a configuration change, bouncing an interface in production, reloading a switch — the orchestration mode shifts. A planner produces an explicit plan that is inspectable and auditable before execution. An actor executes one step. A verification ReAct loop confirms the step succeeded as expected. Control returns to the planner if verification fails or if the step revealed information the plan did not anticipate. This pattern combines the discipline of explicit planning with the reactivity of tight verification, and it matches what production change-automation systems have used for years.
>
> Design principle. Orchestration pattern is selected by the *shape* of the next action (exploratory, localization, or commit-and-verify) and modulated by the *blast radius* (whether human review is warranted before the agent proceeds). Investigation and action are interleaved in real troubleshooting; the orchestration must support that interleaving. The shape axis selects ReAct, Bisection, or Planner-Actor; the blast-radius axis gates whether the action requires Planner-Actor regardless of underlying shape.

### Rationale

Two synthesis Priority 2 items folded into one update because they're inseparable in §3.6: naming the Router/Classifier (synthesis 5 / 2.2) and adding bisection as a third orchestration pattern (synthesis 9 / 2.6). The Router/Classifier becomes the dispatch mechanism for selecting among the three patterns; without it, selection is implicit. The "shape" axis (exploratory/localization/commit-and-verify) is added alongside the "blast radius" axis, which strengthens the §3.6 design principle without breaking the v0.4 framing.

---

## Update 9 — §3.7 Gates (gate-denial contract; investigation-checkpoint primitive)

### Current text in v0.4

> 3.7 Verification and Action Gates
>
> What protects the user, the system, and the agent itself. Gates are the agent's conscience.
>
> In HarnessIT. Every write action passes through a gate appropriate to its blast radius. Low-blast-radius reversible actions execute under the ReAct loop with logging. Higher-blast-radius actions go through dry-run preview, diff visualization, human approval, staged application, post-condition verification, and automatic rollback on regression. Read actions are unrestricted; consequential actions are slow on purpose. Doppelgänger supports the same gating model as a production deployment would, so the lessons transfer.
>
> Design principle. The agent's job is to recommend with provenance, not to act unilaterally on consequential changes. Even when the agent is empowered to act, the action layer should make every action reviewable, reversible, and auditable. The slowest part of a write should be the part where a human can say no.

### Proposed v0.5 text

> 3.7 Verification and Action Gates
>
> What protects the user, the system, and the agent itself. Gates are the agent's conscience.
>
> In HarnessIT. Every write action passes through a gate appropriate to its blast radius. Low-blast-radius reversible actions execute under the ReAct loop with logging. Higher-blast-radius actions go through dry-run preview, diff visualization, human approval, staged application, post-condition verification, and automatic rollback on regression. Read actions are unrestricted; consequential actions are slow on purpose. Doppelgänger supports the same gating model as a production deployment would, so the lessons transfer.
>
> **Gate-denial contract.** When a gate denies an action, the agent does not silently retry, escalate ad hoc, or stall. The gate's denial returns a structured `GateDenial` payload to the orchestration loop:
>
> ```
> GateDenial {
>   action: <the action that was proposed>
>   reason: <human-readable; one of {scope, policy, blast_radius, post_condition_unmet, ...}>
>   reason_class: <enum of contract reasons>
>   reviewable: <bool — is human review available for this denial?>
>   alternatives: <optional list of allowed action shapes the agent could try instead>
>   denial_id: <stable identifier for this specific denial>
> }
> ```
>
> The orchestrator decides the next move based on `reason_class`: a scope denial typically terminates the investigation thread (the agent is reasoning outside its declared scope); a policy denial triggers an escalation request through the human-review path if `reviewable` is true and the orchestrator's escalation budget has not been exhausted; a `post_condition_unmet` denial returns control to a verification ReAct loop. The denial is captured in the trajectory and is itself a finding the eval set may score.
>
> **Investigation-checkpoint primitive.** Gates that require human approval mean agent runs are days, not minutes. v0.4 does not specify where paused-investigation state lives during the wait; v0.5 commits to an explicit primitive.
>
> An *investigation checkpoint* is a serialized snapshot of:
>
> - The orchestrator state (pattern, current step, hypothesis-space partition for bisection, plan progress for Planner-Actor)
> - The CSM working set (the active context window's contents and budget allocations)
> - The short-term memory state (observations, ruled-outs, current hypothesis)
> - The pending action that triggered the gate, with its `GateDenial` reason
>
> Checkpoints are persisted to RememberIT's `harnessit-agent-memory` context with a checkpoint-typed envelope and a stable resumption identifier. When the gate's external review completes (approval, rejection, or expiration), the orchestrator loads the checkpoint, applies the review outcome, and resumes from the gate boundary. LangGraph's checkpointer is the prior-art pattern; HarnessIT inherits the shape but persists into RememberIT rather than into LangGraph's native checkpoint backend, so the substrate stays consistent with the rest of the memory architecture.
>
> Without checkpoint primitives, a multi-day human-approval gate either holds an entire process resident (impractical) or loses investigation state (operationally unsafe). Checkpoints make day-scale gating tractable.
>
> Design principle. The agent's job is to recommend with provenance, not to act unilaterally on consequential changes. Even when the agent is empowered to act, the action layer should make every action reviewable, reversible, and auditable. The slowest part of a write should be the part where a human can say no — and the harness has to be operationally sound during the wait.

### Rationale

Two synthesis items folded into §3.7 (Priority 2 #10): the gate-denial contract (synthesis 2.7) and the investigation-checkpoint primitive (also 2.7). Both are pure specifications of behavior the v0.4 architecture implies but does not commit to. Without the gate-denial contract, gate denial is undefined and the agent's recovery path is ad hoc. Without the checkpoint primitive, multi-day gating has no architectural home. Both are in scope for v0.5 per decision E.

---

## Update 10 — §3.8 Evals (governance + eval-discipline propagation)

### Current text in v0.4

> 3.8 The Discipline Layer: Evals and Trajectories
>
> Wrapping all seven components is the discipline that distinguishes serious harness work from demos.
>
> Evals. A growing test set of network troubleshooting scenarios with known good resolutions, open from day one and accepting community contributions. Every change to any harness component runs against the eval set. Regressions block changes. Improvements are quantified. Evals are the only honest answer to "is the harness getting better."
>
> Trajectories. Every agent run is captured as a trajectory: the inputs, the tool calls, the model outputs, the final resolution. Trajectories are the substrate for debugging, for eval set growth, and for the rare cases where a small fine-tune does pay off (distillation of a successful trajectory into a faster, cheaper model). They are also how the agent's reasoning is made auditable for the operators who will live with its recommendations.
>
> A harness without evals is a demo. A harness with evals is a product.

### Proposed v0.5 text

> 3.8 The Discipline Layer: Evals and Trajectories
>
> Wrapping all seven components (and the orchestration sub-components named in §3.6) is the discipline that distinguishes serious harness work from demos.
>
> Evals. A growing test set of network troubleshooting scenarios with known good resolutions, open from day one and accepting community contributions. Every change to any harness component runs against the eval set. Regressions block changes. Improvements are quantified. Evals are the only honest answer to "is the harness getting better."
>
> Trajectories. Every agent run is captured as a trajectory: the inputs, the tool calls, the model outputs, the final resolution. Trajectories are the substrate for debugging, for eval set growth, and for the rare cases where a small fine-tune does pay off (RFT, DPO, or distillation of a successful trajectory into a faster, cheaper model — see Claim 2 in §2). They are also how the agent's reasoning is made auditable for the operators who will live with its recommendations.
>
> **Eval governance.** Real eval discipline has multiple dimensions that v0.4 collapses into "running against the test set." v0.5 names them:
>
> - **Curation.** Who adds scenarios, against what criteria, with what sign-off. Eval scenarios that the agent saw during development cannot also be the scenarios it is scored against; growth must come from sources outside the development loop.
> - **Scoring stack.** Three-layer: rule-based scoring for objective criteria (did the agent identify the injected failure? did the proposed fix resolve the symptom?), LLM-as-judge for procedural criteria (did the agent follow disciplined investigation steps; did it bisect efficiently?), and human spot-check for high-stakes scenarios on a sample. The stack is honest about which layer scored which dimension.
> - **Contamination prevention.** A held-out blind set the development loop never sees, with rotation discipline. Eval-set leakage into training-loop or trajectory-distillation work is the failure mode that quietly invalidates eval results; the blind set is the firewall.
> - **Trajectory-replay-as-test, distinct from trajectory-as-fine-tune-substrate.** A trajectory replayed against a new harness build is a regression test; a trajectory selected for distillation is training data. The same artifact serves both purposes, but the disciplines are different — replay needs deterministic substrate behavior (Doppelgänger §6.2 underwrites this); distillation needs eval-passing trajectories with low-token-cost equivalents.
>
> The mechanics of governance — specific scoring rubrics, rotation policy for the blind set, contribution gates — are stage-2-onward design work, not v0.5 doc work. v0.5 names the dimensions; the build fills them in.
>
> **Eval-discipline finding propagated from Doppelgänger.** The 2026-05-02 fork spike surfaced an eval-discipline finding that is architectural, not Doppelgänger-specific: aggregate flow-completion-time statistics actively mislead under incomplete-flow conditions. With 0.1% silent drops on a short sim, four flows did not complete; their records were absent from the trace. The remaining flows reported a *lower* median completion time than baseline because the four absent flows were the slowest. Aggregate FCT comparison reported "the injected run was 17% faster" — the precise opposite of the truth. Full memo: `doppelganger/spike/decision_memo.md`, Finding #1.
>
> The finding generalizes. Any post-mortem trace analysis with selection bias from incomplete operations has the same risk. HarnessIT's eval scoring must therefore include three substrate-level commitments:
>
> - **Flow-count delta is a primary failure signature**, not a derivative metric. Eval scoring compares completed-operation counts before comparing operation-time distributions; a count delta is itself a finding.
> - **Compare distributions, not means.** Tail behavior (p99, p99.9, max) is where pathologies show up; means are systematically pulled around by missing-flow censoring. Eval scoring uses distribution-aware comparison primitives by default.
> - **Annotate incomplete operations.** Every operation the eval scenario *intended to run* produces a record, including those that did not complete. The agent (and human eval) can ask "did this complete? if not, why not?" rather than silently treating absence as nonexistence. Doppelgänger's Per-Flow Records (Doppelgänger v0.2 §4.2) carry this commitment at the substrate boundary; HarnessIT's eval-scoring layer extends it to the harness boundary.
>
> A harness without evals is a demo. A harness with evals — and eval discipline that survives incomplete-operation conditions — is a product.

### Rationale

Two synthesis items folded into §3.8: eval governance (synthesis 11) and the eval-discipline propagation from Doppelgänger spike Finding #1. The governance paragraph is intentionally a naming pass, not a full mechanics spec, per decision C — full mechanics are stage-2-onward work. The eval-discipline finding is the most consequential propagation from the spike: it is not Doppelgänger-specific, and a HarnessIT that scored evals on naive aggregate statistics would silently produce wrong answers. The three substrate-level commitments are direct propagations from the Doppelgänger v0.2 §6.3 update memo.

---

## Update 11 — §4.1 Substrate Tier (Substrate Adapter; Driver/Adapter; RememberIT same-author)

### Current text in v0.4

> 4.1 The Substrate Tier (5 dependencies)
>
> Beneath the harness, but consumed by it, are five systems that hold ground truth, provide the means to act, and capture what happens. These are external dependencies of HarnessIT, not part of HarnessIT itself. Each one is a serious system that already exists; reusing them rather than rebuilding is a deliberate choice that lets HarnessIT focus on the agentic logic that makes them work together.
>
> A sensing layer provides observations of the fabric, topology, telemetry, configuration, anomalies. HarnessIT consumes one. It does not own one. The sensing layer can be Doppelgänger (initially), a real product, or a contract layer over multiple sensing systems.
>
> A knowledge corpus provides the documents, specifications, and runbooks that retrieval queries against. HarnessIT curates the corpus for its domain.
>
> An action surface provides the means to apply changes when verification gates approve them. In Doppelgänger, this writes to the simulated fabric. In production, it would write through a managed change pipeline.
>
> A memory substrate (RememberIT) provides persistent storage for both agent operational memory and build-process capture, with client-side encryption and an MCP interface. Two namespaces, one substrate.
>
> A tracing substrate (Langfuse, self-hosted) provides OpenTelemetry-native trace capture for every model call, tool invocation, retrieval, memory access, skill load, gate decision, and verification check. Self-hosted from the start so the project is genuinely cloneable and runnable, and so HarnessIT encounters Langfuse's real operational characteristics rather than smoothing them over with a managed service.

### Proposed v0.5 text

> 4.1 The Substrate Tier (5 dependencies)
>
> Beneath the harness, but consumed by it, are five systems that hold ground truth, provide the means to act, and capture what happens. These are external dependencies of HarnessIT, not part of HarnessIT itself. Each one is a serious system that already exists; reusing them rather than rebuilding is a deliberate choice that lets HarnessIT focus on the agentic logic that makes them work together.
>
> **The Substrate Adapter pattern.** The sensing layer and the action surface are not consumed directly; they are consumed through a named architectural role called a **Substrate Adapter**. A Substrate Adapter wraps an underlying network simulator, emulator, or fabric, and exposes it through HarnessIT's MCP tool contract. Doppelgänger is the first Substrate Adapter (it wraps NS-3 with the inet-tub RDMA additions); a planned NVIDIA AIR Adapter wraps NVIDIA AIR's Cumulus-on-VM emulation; a future SONiC Adapter would wrap SONiC running in containers or on hardware. HarnessIT does not know which Substrate Adapter is providing data — that is the contract's purpose. The adapter pattern is what makes substrate substitution a matter of writing an adapter rather than re-architecting the harness.
>
> Internally, every Substrate Adapter has the same two-layer structure: a **Driver** that talks to the underlying substrate in its native idiom, and an **Adapter shell** (an MCP server) that imports the Driver and exposes its methods as MCP tools. The split exists for testability (the Driver is reusable from a REPL or unit test without MCP scaffolding), separation of concerns, and reusability of the Driver by sibling consumers like ProtoViz that read substrate outputs directly without going through MCP. Doppelgänger's design document covers this structure in detail; HarnessIT cares about the contract the Adapter exposes, not the internal split — but the split is what makes the contract clean.
>
> **The five dependencies.**
>
> *A sensing layer*, consumed through a Substrate Adapter, provides observations of the fabric: topology, telemetry, configuration, anomalies. HarnessIT consumes one. It does not own one. The sensing layer can be Doppelgänger (initially, via the Doppelgänger Substrate Adapter), NVIDIA AIR (via the AIR Adapter, planned), or a real product wrapped by a future Substrate Adapter.
>
> *A knowledge corpus* provides the documents, specifications, and runbooks that retrieval queries against. HarnessIT curates the corpus for its domain.
>
> *An action surface*, also consumed through a Substrate Adapter, provides the means to apply changes when verification gates approve them. In Doppelgänger, this writes to the simulated fabric. In AIR, this writes through the SSH or REST channel to the Cumulus instances. In production, it would write through a managed change pipeline behind its own Substrate Adapter.
>
> *A memory substrate (RememberIT)* provides persistent storage for the agent's operational memory, with client-side encryption and an MCP interface. **Honest framing under Principle 7: RememberIT is provandal.dev's own production MCP service, not the work of an unrelated third-party team. HarnessIT dogfoods RememberIT as its memory substrate.** This is dogfooding by the project's principal engineer; the architecture is open about that. v0.4 framed RememberIT as "the work of other teams" alongside Langfuse (in §10.1); v0.5 corrects the framing here and in §10.1.
>
> *A tracing substrate (Langfuse, self-hosted)* provides OpenTelemetry-native trace capture for every model call, tool invocation, retrieval, memory access, skill load, gate decision, and verification check. Self-hosted from the start so the project is genuinely cloneable and runnable, and so HarnessIT encounters Langfuse's real operational characteristics rather than smoothing them over with a managed service.

### Rationale

Three changes in §4.1: (a) introduce Substrate Adapter as a named architectural role with the Driver/Adapter internal-split note (session-B refinement; the AIR/SONiC framing follows from the role being named); (b) acknowledge RememberIT as a same-author dependency under Principle 7 honesty (synthesis 13); (c) update the "two namespaces" RememberIT note to "one namespace" since the second context (`harnessit-build-journal`) was retired by the v0.3 capture-protocol decision. All three are honesty fixes.

---

## Update 12 — §5 Multi-Agent (downgrade)

### Current text in v0.4

> 5. Multi-Agent from Day One, Used Lightly at First
>
> HarnessIT is designed for multi-agent decomposition from the start. The orchestration layer can route work across multiple specialized agents. Each agent has a defined role, a bounded set of tools, and a clean contract with the orchestrator. Agents that talk to each other do so through structured messages, not free-form chat.
>
> This does not mean HarnessIT v1 ships with a swarm. "Multi-agent from day one" means the architecture supports decomposition; "used lightly at first" means v1 may run with one or two specialized agents (a planner agent and an executor agent, for instance) and grow from there as scenarios demand it. Multi-agent is a capability, not an obligation. The discipline that matters is bounded roles, structured communication, and clean orchestration, not how many agents are running at any given time.
>
> The failure mode to avoid is multi-LLM-call-chaos: many agents arguing inconclusively while burning tokens. HarnessIT's multi-agent design is structured collaboration with clear ownership, not free-form negotiation. The orchestrator owns the work; agents are the orchestrator's deputies.

### Proposed v0.5 text

> 5. Multi-Agent as a Future Extension
>
> v0.5 of HarnessIT is single-agent with the Planner-Actor and Bisection orchestration patterns from §3.6. The architecture is *designed* to support multi-agent decomposition — bounded roles, structured communication, an orchestrator that owns the work and delegates to deputies — but v0.5 does not ship multi-agent and does not commit to a specific multi-agent contract.
>
> Why downgrade from v0.4. v0.4 framed multi-agent as "from day one, used lightly at first," which is hedge language: the project gets to claim the architectural property without paying the design cost. The 2026-05-01 reviewer pressure-test flagged this honestly. Real multi-agent requires a committed coordination contract — supervisor-delegation (LangGraph), shared blackboard, message-passing, or peer negotiation — and committing to one in v0.5 would be premature. None of v0.5's planned scenarios require multi-agent; the orchestration patterns in §3.6 cover them.
>
> What multi-agent looks like as a future extension. When scenarios surface that genuinely need agent decomposition — for example, an investigation that runs in parallel against multiple substrate adapters — HarnessIT will commit to a coordination contract and add a multi-agent section to the architecture. The candidate contract is supervisor-delegation in the LangGraph style, because it matches §3.6's existing "orchestrator owns the work; agents are the orchestrator's deputies" framing. But the choice is deferred until the build evidence supports it.
>
> The failure mode this downgrade avoids: shipping a multi-agent capability that is unused for v1, encoded only in architectural diagrams, and then discovering at v2 that the actual scenario needs a different coordination contract than the one designed-for-but-not-built.

### Rationale

Synthesis 1.9 / 14: per decision B, downgrade rather than commit. The hedge language in v0.4 is the failure mode the synthesis identified; v0.5 owns it explicitly. The "future extension" framing keeps the architectural option open without paying the design tax now.

---

## Update 13 — §6.1 Doppelgänger (post-mortem-only; live-feel; Substrate Adapter)

### Current text in v0.4

> 6.1 Doppelgänger: The Simulated Fabric
>
> HarnessIT is not built against real switches. It is built against a local fabric simulator named Doppelgänger. The name is chosen deliberately: a doppelgänger is a non-biological double, a counterpart of a living thing, which is exactly what the simulator is to a real fabric.
>
> Why simulated. Real switches gate the project on hardware access. They make failure injection difficult or impossible: you cannot ask a real switch to produce a microburst at exactly this moment. They make reproducibility hard. They make the project less accessible to readers who want to clone and run. A simulator solves all four problems.
>
> Why a custom simulator instead of NVIDIA AIR. AIR is excellent for what it does, running real network operating system images in cloud-hosted VMs with simulated wire-level connectivity. But AIR is cloud-hosted (friction for readers), event-driven and physical (limits failure injection), and minutes-to-spin-up (slows iteration). Doppelgänger runs locally, allows scripted failure injection, and starts in seconds.
>
> AIR as a future drop-in target. Doppelgänger exposes the same interface that an AIR adapter would expose. Late in the blog series, an AIR adapter is added as a one-post bonus, running the same harness against a real Cumulus fabric in AIR, which gives the credibility moment without gating the series on AIR access. The harness does not know whether it is talking to Doppelgänger or to AIR underneath.
>
> Doppelgänger as a project of its own. Doppelgänger is a separate deliverable with its own design document. It is open-source, has utility beyond HarnessIT, and is intended to integrate with protocol visualization tooling so readers can see the actual frames the agent is reasoning about. Doppelgänger's design is the third document in this set.

### Proposed v0.5 text

> 6.1 Doppelgänger: The First Substrate Adapter
>
> HarnessIT is not built against real switches. It is built against a local fabric simulator wrapped by HarnessIT's first **Substrate Adapter** (§4.1) — Doppelgänger. The name is chosen deliberately: a doppelgänger is a non-biological double, a counterpart of a living thing, which is exactly what the simulated fabric is to a real one.
>
> Why simulated. Real switches gate the project on hardware access. They make failure injection difficult or impossible: you cannot ask a real switch to produce a microburst at exactly this moment. They make reproducibility hard. They make the project less accessible to readers who want to clone and run. A simulator solves all four problems.
>
> Why a custom simulator instead of NVIDIA AIR for the initial substrate. AIR is excellent for what it does — running real network operating system images in cloud-hosted VMs with simulated wire-level connectivity. But AIR is cloud-hosted (friction for readers), real-time and physical (limits scripted failure injection), and minutes-to-spin-up (slows iteration). Doppelgänger runs locally, allows scripted failure injection, and starts in seconds.
>
> The fundamental limitation Doppelgänger introduces. Doppelgänger runs simulations to completion and exposes the recorded artifacts; the agent investigates against post-simulation state, not live state. This is honest in Doppelgänger's own design document (§1.2 *Not a real-time fabric*) and is real architectural commitment, not incidental. The reviewer pressure-test flagged a consequence: a harness that develops only against post-mortem artifacts will not develop the disciplines that live troubleshooting requires — counter-clearing rituals, propagation-wavefront-watching, time-pressure prioritization, the *did-clearing-the-buffer-help* reflex. v0.5 acknowledges this honestly.
>
> Mitigations within Doppelgänger. Doppelgänger v0.2 specifies a *live-feel replay mode* (in `doppelganger/docs/Doppelganger_Design_v0.2.docx` §2.4 and the mitigation roadmap): even though the simulation is pre-computed, the agent's *exposure* to the simulation can be event-by-event, gated on advancing wall-clock time, with no rewind. Some eval scenarios run in this mode by default to exercise live-troubleshooting disciplines. Doppelgänger also supports adversarial telemetry injection at the response-envelope level (gaps, conflicts, stale data) so confidence/freshness reasoning (§7.5) is exercised before a real-fabric substrate is in the picture.
>
> AIR as the second Substrate Adapter. The AIR Adapter is the project's planned second Substrate Adapter, introduced late in the build sequence. It satisfies the same MCP contract Doppelgänger does, so HarnessIT does not know which substrate is providing data. The transition from Doppelgänger to AIR is one of the series' most instructive moments — readers see the same agent doing the same investigation work across two materially different substrates with the harness unchanged. Future Substrate Adapters (a SONiC Adapter, a real-fabric adapter) follow the same pattern.
>
> Doppelgänger as a project of its own. Doppelgänger is a separate deliverable with its own design document (`provandal/doppelganger`). It is Apache-2.0 licensed, has utility beyond HarnessIT, and is intended to integrate with protocol visualization tooling so readers can see the actual frames the agent is reasoning about. Doppelgänger's design is the third document in this set; the v0.2 update memo (`provandal/doppelganger/docs/v0.2_updates.md`) captures the post-spike refinements.

### Rationale

Three things land in §6.1: (a) re-frame Doppelgänger as the first Substrate Adapter rather than as a one-off (session-B refinement; matches §4.1's new pattern naming); (b) acknowledge post-mortem-only as a fundamental limitation under Principle 7 honesty, with the live-feel replay and adversarial-telemetry mitigations named (synthesis 1.5 — convergent across three reviewers); (c) cross-reference the Doppelgänger v0.2 update memo so the architecture stays in sync with the substrate-side document. The "AIR as future drop-in" → "AIR as second Substrate Adapter" reframing is consistent with §4.1's pattern naming.

---

## Update 14 — §9.4 OpenTelemetry Semantic Conventions

### Current text in v0.4

> 9.4 OpenTelemetry Semantic Conventions
>
> OpenTelemetry has emerging semantic conventions for AI agents — standardized span names, attribute keys, and parent-child patterns. HarnessIT follows these conventions where they exist. Where they do not yet exist, HarnessIT uses sensible names and contributes the conventions back to the OTel community when they prove useful.
>
> Following the conventions has a compounding benefit: HarnessIT traces are consumable by any OTel-compatible tool, not just Langfuse. A reader who later wants to ship traces to Phoenix, Datadog, or a custom backend can do so without reinstrumenting the agent. The instrumentation layer is portable.

### Proposed v0.5 text

> 9.4 OpenTelemetry Semantic Conventions
>
> OpenTelemetry has emerging semantic conventions for AI agents — standardized span names, attribute keys, and parent-child patterns. HarnessIT follows these conventions where they exist.
>
> Where they do not yet exist, HarnessIT uses a stable internal namespace under the `harnessit.*` prefix: `harnessit.skill.load`, `harnessit.skill.invoke`, `harnessit.gate.decision`, `harnessit.gate.denial`, `harnessit.checkpoint.write`, `harnessit.checkpoint.resume`, `harnessit.csm.evict`, `harnessit.classifier.dispatch`, and similar. The `harnessit.*` namespace is documented in the project repository and is intended to be stable across the project's lifetime. Migration to standardized OTel conventions, when (and if) they cover these dimensions, is mechanical: a translation layer at the trace-emission boundary maps `harnessit.skill.load` to whatever the OTel convention names it, with no instrumentation changes inside the harness.
>
> v0.5 does not commit to upstream OTel standards-engagement. v0.4 said HarnessIT would "contribute the conventions back to the OTel community when they prove useful"; the project's open question §13 separately concedes that standards engagement is "vast amounts of time for marginal return when done as a side effort." v0.5 resolves the contradiction by stating clearly: HarnessIT tracks OTel conventions, accommodates them when they ship, and uses a stable internal namespace where they don't. The project does not budget for upstream standards work.
>
> Following whatever conventions exist (OTel or `harnessit.*`) has a compounding benefit: HarnessIT traces are consumable by any OTel-compatible tool, not just Langfuse. A reader who later wants to ship traces to Phoenix, Datadog, or a custom backend can do so without reinstrumenting the agent. The instrumentation layer is portable.

### Rationale

Synthesis 12: v0.4's §9.4 contradicts §13's "track-and-accommodate" stance on standards work. v0.5 owns the contradiction by committing to a stable internal namespace and dropping the "contribute back" language. The migration story — translation layer at trace-emission boundary — keeps the door open without committing time the project doesn't have.

---

## Update 15 — §10.1 Landscape (RememberIT honesty)

### Current text in v0.4

> 10.1 What HarnessIT Is Not Trying To Be
>
> Not a product. HarnessIT is a reference implementation and a teaching artifact. Vendors building products in this space, Forward Networks, Aviz, Cisco, Arista, NVIDIA, and others, have engineering investments and customer relationships HarnessIT will not match. HarnessIT is what those products' architects might read on a Sunday afternoon to sharpen their thinking.
>
> Not a sensing layer. HarnessIT consumes a sensing layer. The sensing layer is somebody else's problem; there are several good ones already, and a separate piece of work addresses how to abstract over them.
>
> Not a fine-tuning project. HarnessIT runs on whatever frontier base model is current. Model selection is a runtime decision, not an architectural one.
>
> Not a tracing or memory system. HarnessIT consumes Langfuse for tracing and RememberIT for memory. These are the work of other teams; HarnessIT integrates them.

### Proposed v0.5 text

> 10.1 What HarnessIT Is Not Trying To Be
>
> Not a product. HarnessIT is a reference implementation and a teaching artifact. Vendors building products in this space — Forward Networks, Aviz, Cisco, Arista, NVIDIA, and others — have engineering investments and customer relationships HarnessIT will not match. HarnessIT is what those products' architects might read to sharpen their thinking.
>
> Not a sensing layer. HarnessIT consumes a sensing layer through a Substrate Adapter (§4.1). The sensing layer is somebody else's problem; HarnessIT defines the contract.
>
> Not a fine-tuning project. HarnessIT runs on whatever frontier base model is current. Model selection is a runtime decision, not an architectural one. (Selective post-training of trajectories — RFT, DPO, distillation — is in scope per Claim 2; the architecture is harness-first, not anti-fine-tuning.)
>
> Not a tracing system. HarnessIT consumes self-hosted Langfuse for tracing. Langfuse is the work of other teams; HarnessIT integrates it.
>
> Not a from-scratch memory system. HarnessIT consumes RememberIT for memory. **RememberIT is provandal.dev's own production MCP service, not the work of an unrelated third party.** HarnessIT dogfoods RememberIT — Principle 7 (honesty about hard parts) requires that the architecture be candid about this dependency rather than framing it alongside Langfuse as if both are external. The dogfooding is deliberate and operationally useful (it forces the memory substrate to meet real workloads), but it is dogfooding, not third-party consumption.

### Rationale

Synthesis 1.7 / 13: v0.4 framed RememberIT alongside Langfuse as "the work of other teams," which the agentic reviewer flagged as misleading because RememberIT is the author's own service. The rest of the paragraph stays; the RememberIT line is rewritten honestly. This update mirrors the §4.1 RememberIT-honesty change for §10.1, since both sections needed it.

---

## Update 16 — §11 Roadmap (stage-count mismatch)

### Current text in v0.4

§11 currently presents an 8-stage roadmap (stages 0–7) organized as editorial sections, each producing "code and writing." Build Plan v0.2 §2.1 separately presents a 14-stage roadmap (stages 0–13) organized as tagged commits.

### Proposed v0.5 text (insert as new §11.0 paragraph at the start of §11, before the stage table)

> §11.0 Two stage maps, one project. The Architecture's stage list (below) is organized around the *editorial sections of the published series* — eight stages corresponding to the eight acts a reader experiences. The Build Plan (§2.1 of `HarnessIT_BuildPlan_v0.x`) presents a 14-stage list organized around *tagged commits in the build sequence*. The two are operating at different granularities: editorial-act granularity for the Architecture; build-commit granularity for the Build Plan.
>
> The mapping is roughly two-build-stages-to-one-editorial-stage on average, with some 1:1 and some 3:1 alignment. The Build Plan owns the canonical mapping table (Build Plan v0.3 §2.4, when published). Readers who want the implementation cadence consult the Build Plan; readers who want the narrative cadence consult the Architecture.
>
> No stage in either list crosses the 60/4/12 plan-of-record tier boundary except by reference: tier boundaries are a Build Plan concern, not an editorial concern.

### Rationale

Synthesis 1.8 / Priority 1 #4: the 8-vs-14 mismatch is real and will confuse a reader. The fix is one paragraph that names both granularities and points at the canonical mapping. v0.5 adds the paragraph in the Architecture; Build Plan v0.3 §2.4 owns the actual table. This is fixable in roughly the time the synthesis estimated (30 minutes); the paragraph above is most of that time.

---

## Update 17 — §13 Open Questions (convert punts or strike)

### Current text in v0.4

> 13. Open Questions for v0.4
>
> This document is v0.4. The following questions remain open and invite pushback before v1.0:
>
> IETF NMRG engagement model. Track-and-accommodate is the v0.3 posture. The project will contribute observations and gap reports if the work surfaces them, but will seek collaborators willing to carry contributions through the standardization process rather than carrying them ourselves. Standards work consumes vast amounts of time for marginal return when done as a side effort.
>
> Eval set scope at launch. "Big enough to be useful" is the right goal, but the actual size cannot be determined until the harness is far enough along to see what fails interestingly. The eval set grows organically with the project; the launch number is whatever is honestly defensible at that point.
>
> Skill format. The format question reduces to "what does the model consume best." If raw markdown with light convention works as well as structured YAML for model consumption, markdown wins on authoring friction. If structured formats demonstrably improve model behavior, a skill compiler that authors in markdown and emits the structured form is the path. The decision is empirical and will be made in the early posts of the series with measurements.
>
> Trajectory viewer as dynamic sequence diagram. The proposed design renders trajectories as ProtoViz-style sequence diagrams: services as column headings, time flowing downward, interactions as arrows between columns as the investigation unfolds. This handles long investigations naturally because temporal density is visible at a glance, parallel agent activity is legible, and the reader can zoom in on specific time windows. This becomes the v0.3 working design unless something better surfaces during build.
>
> Model recommendation matrix. Open. The project will form opinions through use and share them when they crystallize. For now, model selection is a runtime choice the user makes, documented but not prescribed.

### Proposed v0.5 text

> 13. Open Questions for v0.5
>
> This document is v0.5. The following questions remain open and invite pushback before v1.0. v0.5 distinguishes between *design questions* (answers we owe before stage 0 begins), *empirical questions* (answers that will surface in the build), and *operational questions* (decisions deferred until they are forced).
>
> **Design questions (resolve before stage 0).**
>
> - *None as of v0.5.* The questions v0.4 listed under this heading have either been resolved (substrate fork — see Doppelgänger v0.2 §9.2; multi-agent — see §5; HPCC vs. inet-tub — same) or moved to the empirical / operational categories below.
>
> **Empirical questions (the build will produce the answer).**
>
> - **Skill format.** Markdown with light convention vs. structured YAML — the decision reduces to what the model consumes best. The early stages of the build will measure both; whichever produces better eval scores at comparable token cost wins. Expected resolution: stage 5 of the Build Plan.
> - **Eval set scope at launch.** "Big enough to be useful" is the right goal; the actual size emerges as scenarios are written and exercised. Expected resolution: stage 6.
> - **Retrieval upgrades** (hybrid retrieval, reranking, query rewriting). v0.5 acknowledges these are missing (§3.2); v0.6 or stage 6 absorbs them depending on which arrives first.
>
> **Operational questions (deferred until forced).**
>
> - **IETF NMRG engagement model.** Track-and-accommodate is v0.5's stance; the project does not budget for upstream standards work (§9.4). If a contributor surfaces who is willing to carry observations through the standardization process, the project supports them; otherwise the `harnessit.*` namespace is the working compromise.
> - **Trajectory viewer as dynamic sequence diagram.** The proposed design becomes v0.5's working design. If something better surfaces during the build, the viewer changes; v0.5 does not commit beyond the current best understanding.
> - **Model recommendation matrix.** Open. Model selection is a runtime choice documented in the project's `MODELS.md`, not in the architecture. The project forms opinions through use and shares them when they crystallize.

### Rationale

Synthesis 2.9 flagged §13 as punts dressed as openness. v0.5 separates the genuinely-empirical questions from the operationally-deferred ones and strikes the resolved items (substrate fork, multi-agent commitment). The "Eval set scope" item is tagged empirical with an expected-resolution stage so it has a deadline, not a vague "grows organically with the project." Same for skill format. This is honesty under Principle 7 — open questions get treated as design debt, not as a graceful gesture.

---

## What v0.5 does *not* change

For symmetry with `doppelganger/docs/v0.2_updates.md` and `harnessit/docs/v0.3_updates.md`:

- **§3.1 Tool Surface** — clean section per the synthesis review; no major flags.
- **§3.7 mid-paragraphs (gating mechanics)** — the dry-run / approval / staged / verification / rollback shape stays. Only the gate-denial contract and checkpoint primitive are added (Update 9).
- **§7 Security** — strongest section in v0.4 per the synthesis; gates-vs-guardrails distinction is good. No structural changes.
- **§8 Multimodal** — CSM-forced framing is correct per the synthesis; the §8.3 "Why It Forces CSM" subsection stays. No changes; the existing CSM detail level is the public floor (per `feedback_csm_disclosure_line.md`).
- **§9.1 Self-Hosted Langfuse** — decision to self-host from start stays; reasoning unchanged.
- **§9.2 What Gets Traced** — list of traced events stays; new orchestration sub-components (Router/Classifier dispatch, bisection partition state) flow in naturally without restructuring.
- **§9.3 Custom Viewer** — sequence-diagram framing stays. The "killer feature" line in §11.1 may merit softening but is editorial-stylistic, not architectural.
- **§9.5 ProtoViz future addendum** — stays.
- **§11.1 The UI from Day One** — content stays; "killer feature" framing is rhetorical and can survive into v0.5.
- **§12 Principles 1, 2, 3, 4, 5, 7, 8** — all stay. Only Principle 6 is updated (Update 1).
- **§14 Closing** — stays.

---

## Application order

Recommended order for applying these updates to v0.4.docx → v0.5.docx:

1. Header (Update 0) — easy, sets version.
2. §1 + §12 metaphor and clone-and-run (Update 1) — localized.
3. §2 Claims (Updates 2, 3, 4) — sequential; Claim 1 first because it sets the time-indexed framing that Claims 2 and 4 lean on.
4. §3.2 Retrieval (Update 5) — short.
5. §3.3 Skills (Update 6) — most substantial in §3.
6. §3.4 + §3.5 Memory/CSM cleanup (Update 7) — needs both sections open simultaneously to keep cross-references coherent.
7. §3.6 Orchestration (Update 8) — depends on §3.3 for the Router/Classifier ↔ progressive-disclosure cross-reference.
8. §3.7 Gates (Update 9) — depends on §3.4 (checkpoint persisted to memory).
9. §3.8 Evals (Update 10) — depends on §3.6, §3.7 for cross-references.
10. §4.1 Substrate Tier (Update 11) — depends on having §3 stable for the Substrate Adapter cross-references throughout §3.
11. §5 Multi-Agent (Update 12) — depends on §3.6 for the orchestration patterns reference.
12. §6.1 Doppelgänger (Update 13) — depends on §4.1 for Substrate Adapter naming.
13. §9.4 OTel (Update 14) — independent.
14. §10.1 Landscape (Update 15) — depends on §4.1 for the parallel RememberIT honesty edit.
15. §11 Roadmap (Update 16) — depends on §4.1 for Substrate Adapter context.
16. §13 Open Questions (Update 17) — last, because it cross-references most resolved items (§5, §9.4, §11) and the resolved items need to be in place first.

After applying all updates, verification pass:

- Search for "the harness is the car" — should return zero matches.
- Search for "harnessit-build-journal" — should return zero matches (replaced by `journal.md`).
- Search for "two contexts, one substrate" — should be replaced by "one context, one substrate" in §3.4.
- Search for "loaded once at start" — should return zero matches (replaced by progressive disclosure).
- Search for "from day one, used lightly at first" — should return zero matches in §5.
- Confirm "Substrate Adapter" appears in §1.x or §3.x or §4.1 the first time it is used (and is defined there); subsequent uses should not redefine it.
- Confirm `harnessit.*` namespace examples in §9.4 are consistent with whatever the project actually emits in stage 0.
- Confirm the §11 stage-mapping note points at the right Build Plan section number.

That's the v0.5 doc revision in full.
